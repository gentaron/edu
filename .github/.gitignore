# Ignore everything
*
# But not workflows/ and its contents
!workflows/
!workflows/**
!/.gitignore
# ...and not the Dependabot config. Without this exception the file above
# swallows it silently: `git add` reports nothing, and Dependabot never runs
# because GitHub only reads it from the default branch.
!/dependabot.yml
