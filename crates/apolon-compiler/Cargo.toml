[package]
name = "apolon-compiler"
version = "0.1.0"
edition = "2021"
description = "Compiler for the Apolon DSL — card ability language for the edu battle system"

[dependencies]
wasm-encoder = "0.220"
wasmparser = "0.220"

[dev-dependencies]
